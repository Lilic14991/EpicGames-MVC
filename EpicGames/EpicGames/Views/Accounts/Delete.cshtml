@model EpicGames.Models.Account
@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

@using (Html.BeginForm("Delete", "Accounts"))
{
    <div>
        <p>Confrm if you want to delete this account</p> 
    </div>

    <input name="confirmButton" type="submit" value="Delete"
           onclick="return confirm('Proceeding with delete action to record with id = @Model.AccountID');" />
}